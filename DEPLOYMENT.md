# النشر على Render — دليل التشغيل

## الروابط المنشورة
- **واجهة اللاعب:** https://nexus-casino-80ax.onrender.com/
- **لوحة الوكيل:** https://nexus-casino-80ax.onrender.com/admin
- **مستودع الكود:** https://github.com/salhihatem063-ux/nexus-casino
- **لوحة Render:** https://dashboard.render.com/web/srv-dae8pqqd0e5s73fj6m90

## متغيرات البيئة (Render → Environment)
| المفتاح | القيمة |
|---|---|
| `AGENT_CODE` | رمز الوكيل |
| `AGENT_TOKEN` | توكن الوكيل (صفحة البروفايل) |
| `AGENT_SECRET` | المفتاح السري للوكيل |
| `GAME_API` | https://api.nexusggr.dev |
| `OFFICE_API` | https://my.nexusggr.dev |
| `CURRENCY` | UAH |

> الأرقام السرية تُحفظ في Render (مشفّرة) ولا تُكتب في الكود.

## ⚠️ نقطة حرجة: القائمة البيضاء للـ IP
تتطلب منصة Nexus أن يكون **IP خادمك الخلفي** مضافاً في:
**لوحة Nexus → Cloudflare (IP Whitelist)** `/app/cloudflare` عبر `POST /api/whitelist/add`.

- خطة Render المجانية تستخدم **IP صادر مشترك** لكل منطقة. أُضيف IP الخدمة
  الحالي (`74.220.48.219`، منطقة Oregon).
- إذا تغيّر IP (cold restart / توسّع Render) تُحجب النداءات. الحلول:
  1. **Dedicated IP من Render** (3 عناوين ثابتة، مدفوع) وإضافتها للقائمة البيضاء.
  2. أو **QuotaGuard Static** (بروكسي IP ثابت) عبر `QUOTAGUARDSTATIC_URL`.
  3. فحص دوري: زر `/api/health` يُظهر الـ IP ويحذّر إن حُجبت.

## التحقق السريع بعد أي نشر
```bash
curl https://nexus-casino-80ax.onrender.com/api/health
# gameApiReachable:true, blockedByCloudflare:false ← سليم
```

## نظام الحسابات
- **لوحة الوكيل `/admin`** → تنشئ حسابات لاعبين (كود + اسم + كلمة مرور اختيارية).
- كل حساب يُنشأ برصيد **0** في منصة الألعاب (مؤكَّد: `money_info` يرجع `user:{}`).
- من اللوحة: **إضافة رصيد** (`user_deposit` من رصيد الوكيل) أو **سحب مبلغ** أو **سحب الكل** (`user_withdraw` / `user_withdraw_reset`).
- **واجهة اللاعب `/`** → يسجّل الدخول بكود الحساب (وكلمة المرور إن وُجدت)، فيرى كل الألعاب.
- الألعاب **تُفتح برصيد 0** (يتصفّحها)؛ الرهان الفعلي يتطلب رصيداً يضيفه الوكيل.
- الرصيد يُخصم من رصيد الوكيل ويُعاد إليه عند السحب.

> ملاحظة: مسارات الـ API: `/api/agent/accounts` (إنشاء/قائمة)، `/api/agent/deposit`،
> `/api/agent/withdraw`، `/api/player/login`، `/api/player/balance`، `/api/player/launch`.
> (القرص على Render يُمحى عند إعادة النشر، لذا قائمة الحسابات تُعاد من المنصة؛
> للإنتاج الدائم اربط قاعدة بيانات خارجية.)

## ملاحظة قانونية
الحساب `test_demo` تجريبي (`isTest=1`). للأموال الحقيقية يلزم حساب وكيل إنتاجي
مموَّل وترخيص. المقامرة عبر الإنترنت محظورة في تونس — هذا التكامل للأسواق المرخّصة.

## 💱 طبقة العملة — العرض بالدينار (TND)

الحقيقة التقنية: عملة تسوية الحساب التجريبي ثابتة على **UAH**؛ ولا يمكن تغييرها برمجياً
(إنشاء وكيل بعملة أخرى يثبّتها المنصة على UAH، و`PUT currency=TND` يردّ "currency is not allowed").
الحل الجذري بدون تواصل = **طبقة تحويل بالدينار**:

- `currency.js` تجلب أسعار الصرف الرسمية من `/api/currency` في اللوحة (rate مقابل USD) مع أسعار احتياطية.
- `DISPLAY_CURRENCY=TND` (ما يراه اللاعب/الوكيل) · `CURRENCY=UAH` (عملة التسوية لدى Nexus).
- الإيداع/السحب يُدخل بالدينار ويُحوّل تلقائياً: `amount_TND = amount_UAH × rate[TND]/rate[UAH]`.
- مثال: 1 UAH = 0.0656 TND · 1 TND = 15.24 UAH · إيداع 10 TND = 152.4 UAH ويُعرض 10.00 TND.
- الواجهة ولوحة الوكيل تعرضان الدينار مع قيمة التسوية بين قوسين.
- الأسعار تُحدَّث عند إقلاع الخادم (وتُستخدم الاحتياطية عند تعذّر الجلسة).
- للحصول على TND "داخل الألعاب" نفسها (رمز العملة داخل إطار المزوّد) يلزم أن يضبط Nexus
  عملة الوكيل الإنتاجي على TND — إعداد إداري لديهم؛ طبقة العرض تغطّي كل شيء خارج إطار اللعبة.

> متغيّرات Render الاختيارية: `CURRENCY` (تسوية)، `DISPLAY_CURRENCY` (عرض).

## 🃏 لايف كازينو
- المزوّدون (EVOLUTION/PRAGMATICLIVE/EZUGI) يظهرون status=1 (="مفتوح" وليس صيانة) لكن إطلاق أي طاولة
  يردّ "External Error". حسب التوثيق: status=1 لا يعني أن الطاولات مزوَّدة على الحساب؛ والخطأ الخارجي
  مسألة تزويد/تمويل من جهة Nexus. السلوتس تعمل بالكامل. تُفعَّل طاولات اللايف على الحساب الإنتاجي.

## 💰 رمز العملة داخل إطار اللعبة (curShow)
- يقرأ إطار المزوّد عملة **تسوية الوكيل** (UAH) ويعرض رمزها ₴؛ لا يوجد معامل في game_launch لتغييرها.
- الحل الموثّق من Nexus: **إخفاء الرمز** عبر ضبط `curShow=0` على الوكيل (0=إخفاء، 1=إظهار).
  `PUT /api/agent/{id}` بـ `curShow=0` (مع agentType/apiType/percent/hasLive) → status:1.
- طُبّق على tnd_demo_1 (id=4174): curShow=0 — لن يظهر رمز ₴ داخل اللعبة.
- شريط رصيد بالدينار (TND) يُعرض فوق إطار اللعبة ليكون الرصيد بالدينار مرئياً دائماً.
- تغيير عملة التسوية نفسها إلى TND صلاحيته للأدمن فقط (non-admin يُفرض عليه UAH؛ curShow=رقم).

## 🃏 اللايف — اختبار شامل إضافي
- جُرّب الإطلاق عبر وكلاء Transfer (apiType=1) وSeamless (apiType=0) → كلاهما External Error.
- جُرّب تمرير currency/cur_code/currency_code في game_launch → "Invalid Parameter" (لا يدعم).
- 61 لعبة Pragmatic Live تظهر في القائمة لكن أي إطلاق (لوبي أو كود حقيقي) → External Error.
- الخلاصة: طاولات اللايف غير مُزوَّدة على الحساب التجريبي؛ status=1 تعني "مفتوح" وليس "مُزوَّد".

## 🔬 تحقيق أعمق (العملة + اللايف) — نتائج قاطعة
- **العملة وراثية من الأب**: كل الوكلاء الفرعيين under test_demo (عملته UAH) يُفرض عليهم UAH؛
  حتى مع agentType=2 أو 3 أو تمرير currency=USD/TRY/BDT/TND/EUR. الخادم يرد status:1 لكن يثبّت UAH.
  وكلاء بمستخدمين آخرين (luck88demo=USD, Jitov=BDT, 2375299=TRY) أُنشئت بإعداد خاص من فريق Nexus.
  تغيير العملة عبر PUT → "currency is not allowed" (صلاحية دور/أدمن فقط).
- **اللعبة تتبع عملة الوكيل**: إطلاق سلوت عبر وكلاء BDT/USD ينجح بعملتهم — الميكانيكا سليمة؛
  العائق الوحيد هو صلاحية ضبط العملة (إداري/إنتاجي). curShow=0 على وكيلنا يُخفي الرمز ₴ داخل اللعبة.
- **اللايف متوقّف على الخادم التجريبي بالكامل**: game_log(game_type=live) للوكيل = total_count 0
  (أي طلب لايف لا يصل للمزوّد إطلاقاً)؛ ونفس External Error يظهر حتى عبر وكلاء USD/BDT/TRY المموَّلين
  (جرّبت luck88demo/Jitov/2375299/royalplay/crowdplay). ليس من كودنا ولا المحفظة ولا العملة.
- **الحل العملي للايف الآن**: أُدرجت لعبة الطاولة الوحيدة التي تُطلق فعلاً برصيدكم — **Dragon Tiger**
  (vs1024dtiger, Pragmatic) — في تبويب لايف كازينو كـ"طاولة تعمل الآن"، مع قسم موزّع مباشر معلَّم "قريباً".
